filters:
  and:
    - or:
        - file.name == "Relationship - Camille"
        - category == "relationship"
views:
  - type: table
    name: Next Action
    filters:
      and: []
    order:
      - next-action
      - current-focus
    sort:
      - property: last-updated
        direction: DESC
